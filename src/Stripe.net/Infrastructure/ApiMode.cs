namespace Stripe
{
    public enum ApiMode
    {
        /// <summary>
        /// V1 API mode uses URL-encoded requests and JSON responses.
        /// </summary>
        V1,

        /// <summary>
        /// V1 API mode uses JSON-encoded requests and responses.
        /// </summary>
        V2,
    }
}
